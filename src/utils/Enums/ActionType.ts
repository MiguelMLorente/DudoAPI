export enum ActionType {
    BID = "BID",
    CALL = "CALL",
    SPOT_ON = "SPOT ON",
    CREATE_GAME = "CREATE GAME",
    JOIN_GAME = "JOIN GAME",
    POST_ROUND = "POST ROUND",
    PLAYER_READY = "PLAYER READY",
    KICK_USER = "KICK USER"
}